# Root env for docker-compose (frontend build args). The backend reads its own
# backend/.env. Copy to .env and fill in before `docker compose up --build`.
VITE_BACKEND_URL=https://api.your-domain.com
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
